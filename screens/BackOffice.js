import { ImageContainer, styles } from "../components/neworderstyle";
import { View, Text } from "react-native";

export default BackOffcie = ({ navigation }) => {
  return (
    <ImageContainer source={require("../assets/layout.png")}>
      <View style={styles.container}></View>
    </ImageContainer>
  );
};
